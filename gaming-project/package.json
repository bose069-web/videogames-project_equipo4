{
  "name": "gaming-project",
  "version": "1.0.0",
  "private": true,
  "description": "Professional full-stack gaming platform with React, Express, and Firebase Firestore.",
  "workspaces": [
    "src/client",
    "src/server"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "lint": "npm run lint --workspace src/server && npm run lint --workspace src/client",
    "build": "npm run build --workspace src/client"
  },
  "engines": {
    "node": ">=20"
  }
}
